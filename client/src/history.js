import { createBrowserHistory } from 'history';

//TODO Followed a weblink to do this, not sure why I can't just use it in the routes file, but it doesn't work if I do.
export default createBrowserHistory();
